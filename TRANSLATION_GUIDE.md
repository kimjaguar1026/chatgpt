# 600페이지 PDF 분할 번역 가이드

OCR이 완료된 대용량 PDF는 한 번에 번역하기보다 **페이지나 섹션 단위로 나눠 처리**하는 것이 안전하고 효율적입니다. 아래는 일반적인 작업 흐름과 분할 기준입니다.

## 1) 분할 전략

- **페이지 구간 기준**: 600페이지라면 **30페이지씩 20개 섹션**으로 균등 분할 (예: 1–30, 31–60, …, 571–600)
- **목차/장 기준**: 문서가 장(Chapter) 단위로 구성되어 있다면 장별로 나누기
- **문서 길이 기준**: 번역 도구의 입력 제한(토큰/문자 수)을 고려해 분할하기

## 2) 작업 흐름 예시

1. **페이지 범위 결정**: 600페이지를 30페이지씩 20개 구간으로 나눔
2. **구간별 OCR 텍스트 추출**: PDF에서 해당 페이지 텍스트를 추출
3. **구간별 번역**: 번역 툴에 넣을 수 있는 길이로 잘라 **영어 → 한국어**로 번역
4. **병합 및 검수**: 번역 결과를 원문 순서대로 병합 후 검수

## 3) 품질 유지 팁

- **용어집(Glossary) 만들기**: 문서 전반에 동일 용어가 반복되면 표준 번역어를 고정
- **스타일 가이드**: 존댓말/반말, 기술 문서 톤 등 스타일을 미리 정해 일관성 유지
- **파일명 규칙**: 예) `part_01_p001-050`, `part_02_p051-100` 처럼 정리

## 4) 다음 단계 제안

- 원하는 **분할 기준**(페이지 구간/장 기준)을 알려주면 그에 맞춰 구체적인 분할 계획을 세울 수 있습니다.
- 번역 도구의 **입력 제한**(예: 최대 문자 수)을 알려주면 분할 크기를 최적화할 수 있습니다.

## 5) 바로 테스트할 수 있는 스크립트

아래 스크립트는 OCR 완료 PDF를 **30페이지씩 20개 섹션**으로 나눠 텍스트를 추출하고, 필요하면 **영어 → 한국어**로 번역까지 수행합니다. `input/` 폴더에 PDF를 넣고 바로 실행할 수 있도록 구성되어 있습니다.

```bash
python tools/translate_pdf.py ./input/your.pdf --output ./output
```

번역까지 실행하려면 LibreTranslate 같은 로컬 번역 API가 필요합니다.

```bash
python tools/translate_pdf.py ./input/your.pdf \
  --output ./output \
  --translate \
  --libretranslate-url http://localhost:5000/translate
```

필요 패키지 설치:

```bash
pip install -r requirements.txt
```
